import * as types from '../actions/actionTypes';
import {staffInitState as initState, staffInitState} from './initialState';

const staffReducer = (state=staffInitState, action={}) => {
  return state;
}

export default staffReducer;